# Legacy spatial-statistics notes moved

The consolidated archived guide is published in the
[legacy documentation](https://imcanalysis.readthedocs.io/en/latest/legacy/spatialstats.html).
These scripts are retained only to reproduce old notebook workflows; use the
current `pairsp` or `nxsp` pipeline stage for maintained analyses.
