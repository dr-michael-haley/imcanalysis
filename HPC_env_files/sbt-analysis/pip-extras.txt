# Shared PyTorch/CUDA 12.8 stack. These versions match the working segmentation
# environment and are installed in the same resolver transaction as all four
# migrated runtime families.
numpy==1.26.4
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1
pytorch-lightning==2.6.0

# Precompiled RAPIDS-singlecell CUDA 12 wheel. The RAPIDS/CuPy stack itself is
# supplied by Conda above, so do not request the wheel's [rapids] extra.
rapids-singlecell-cu12==0.16.1

# imc_segmentation requirements.
cellpose==3.1.0
Nimbus-Inference==0.0.4
openai==2.7.1
SpatialDM==0.3.1
opencv-python-headless==4.11.0.86
pyarrow>=15,<23

# NumPy-1/Zarr-2 SpatialData compatibility spine shared with Squidpy and
# Nimbus. SpatialData >=0.5 reaches multiscale-spatial-image 2.0.3 and
# xarray-dataclass >=3, which requires NumPy >=2; Squidpy 1.6.5 also requires
# Zarr <3. Keep this ordered set explicit rather than allowing either family
# to float into the incompatible NumPy-2/Zarr-3 generation.
spatialdata==0.4.0
spatialdata-plot==0.4.0
multiscale-spatial-image==2.0.2
spatial-image==1.2.1
xarray-dataclasses==1.9.1
xarray==2024.11.0
# Final Zarr 2 release; its metadata also enforces Numcodecs <0.16.
zarr==2.18.7
dask==2024.11.2
dask-expr==1.1.19

# imc_biobatchnet requirement, pinned to the commit captured from the working
# HPC environment rather than a moving upstream branch.
BioBatchNet @ git+https://github.com/UoM-HealthAI/BioBatchNet.git@b1d708c62f5bac70f323a36aa28c6057f63e8222

# imc_cellcharter requirements. CellCharter 0.3.7 requires Python >=3.11.
# Its optional TRVAE/proteomics extra is deliberately excluded: the candidate
# supports the default existing-embedding CellCharter route without scArches.
cellcharter==0.3.7
readimc==0.9.2
squidpy==1.6.5
